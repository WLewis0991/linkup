# Deployed API base URL, e.g. https://linkup-api.onrender.com
# Baked in at build time — must be set before `npm run build` for production.
VITE_BACKEND_URL=http://localhost:3000

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
